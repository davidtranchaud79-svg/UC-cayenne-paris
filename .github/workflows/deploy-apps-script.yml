name: Synchroniser Apps Script

on:
  push:
    branches:
      - main
    paths:
      - "src/**"
      - ".github/workflows/deploy-apps-script.yml"
      - "scripts/deploy-apps-script.mjs"
      - "tests/**"
      - "package.json"
      - "package-lock.json"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: apps-script-${{ github.ref }}
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      CLASP_SCRIPT_ID: ${{ secrets.CLASP_SCRIPT_ID }}
      # Public identifier from the application's shared URL; it is not a credential.
      CLASP_DEPLOYMENT_ID: AKfycbzIr2LcGT2_IIR2Vj8h9E8yjxBUGZa7EUoUgwPLZTFt9f4S1bTTiZ9XimqcezTQHNixgg

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Check deployment secrets
        env:
          CLASPRC_JSON: ${{ secrets.CLASPRC_JSON }}
        run: |
          missing=0
          for name in CLASP_SCRIPT_ID CLASPRC_JSON; do
            if [ -z "${!name}" ]; then
              echo "::error::Secret GitHub manquant: $name"
              missing=1
            fi
          done

          if [ "$missing" -ne 0 ]; then
            echo "Ajoutez les secrets GitHub dans Settings > Secrets and variables > Actions."
            exit 1
          fi

      - name: Install clasp
        run: npm install -g @google/clasp@3.4.1

      - name: Install test dependencies
        run: npm ci --ignore-scripts --no-audit --no-fund

      - name: Test interface and deployment verification
        run: npm test

      - name: Write clasp configuration
        env:
          CLASPRC_JSON: ${{ secrets.CLASPRC_JSON }}
        run: |
          node <<'NODE'
          const fs = require('node:fs');
          const os = require('node:os');
          const path = require('node:path');
          const fail = message => {
            console.error('::error::' + message);
            process.exit(1);
          };
          let credentials;
          try {
            credentials = JSON.parse(process.env.CLASPRC_JSON);
          } catch {
            fail('CLASPRC_JSON doit contenir le fichier JSON complet obtenu après clasp login.');
          }
          const token = credentials?.tokens?.default;
          if (token?.type !== 'authorized_user' ||
              ['client_id', 'client_secret', 'refresh_token'].some(key =>
                typeof token[key] !== 'string' || !token[key].trim())) {
            fail('Connexion Google incomplète. Relancez npx --yes @google/clasp@3.4.1 login et recopiez le fichier dans CLASPRC_JSON.');
          }
          const scriptId = process.env.CLASP_SCRIPT_ID.trim();
          if (!/^[A-Za-z0-9_-]+$/.test(scriptId)) {
            fail('CLASP_SCRIPT_ID doit contenir uniquement l’ID du script, sans URL.');
          }
          fs.writeFileSync('.clasp.json', JSON.stringify({scriptId, rootDir: 'src'}), {mode: 0o600});
          fs.writeFileSync(path.join(os.homedir(), '.clasprc.json'),
            JSON.stringify({tokens: {default: token}}), {mode: 0o600});
          NODE

      - name: Check target project before pushing sources
        run: node scripts/deploy-apps-script.mjs --check-only

      - name: Push sources to Apps Script
        run: clasp push --force

      # Updating this deployment through the API reproduced a Google 403 even
      # with unchanged scopes and ANYONE_ANONYMOUS. Keep the owner's published
      # version intact; publish from the Google editor after source sync.
      - name: Indiquer la validation Google restante
        run: |
          cat >> "$GITHUB_STEP_SUMMARY" <<'TEXT'
          ## Sources synchronisées

          Les tests ont réussi et les fichiers ont été envoyés vers Apps Script.
          La version publique n'a pas été modifiée automatiquement.

          Depuis le compte propriétaire : **Déployer > Gérer les déploiements > Modifier > Nouvelle version > Déployer**.
          Conserver **Exécuter en tant que : Moi** et **Accès : Tout le monde**, ainsi que le déploiement existant.

          La publication automatique est suspendue après un refus d'accès Google reproduit après une mise à jour API.
          TEXT

      - name: Remove runner credentials
        if: always()
        run: rm -f "$HOME/.clasprc.json" .clasp.json
